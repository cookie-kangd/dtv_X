# DTV X 更新日志

## v0.1（2026-08-29 全新发布）

### 🎨 应用图标
- 使用用户提供的 dTVX 艺术化设计图：黑色背景 + dTVX 字母 2x2 紧凑排版（d、t 上排，v、x 下排）+ 玫瑰金到蓝紫双色渐变 + 后方金蓝光环弧线
- 替换 androidApp mipmap 全部密度 + screenshots/icon.png

### ⚙️ 设置-基本设置
- **小卡片模式**（默认开启）：3 列紧凑卡片，一屏可展示更多直播间
- **画质档位**（默认最高）：最高（不限制分辨率+强制最高码率）/ 高（1080p）/ 中（720p）/ 低（480p），网络差可降档
- **记住栏目**（默认开启）：斗鱼/虎牙/抖音/B站切换栏目自动记录
- **主题模式**（跟随系统/亮色/暗色）
- **全局颜色**：13 种预设强调色 + 恢复默认

### 🧹 直播间画质（去自动 + 真实最高）
- 斗鱼/抖音/B站画质选项**去掉"自动"**，进房间默认直接选真实最高：
  - 斗鱼：`playInfo.variants` 中 `rate` 最大的档位
  - 抖音：`原画`（ORIGIN）
  - B站：`原画`（qn=10000）
- 抖音/B站**原画解析失败自动降级**到下一档（避免未登录/非大会员时直接黑屏报错）

### 🔄 检查更新
- 设置页一键查询仓库 Release，有新版本时自动下载并唤起安装
- **修复"下载并安装"点击后无反应**：Android 8+ 未授权"允许安装未知应用"时自动跳转到系统设置引导
- 下载 OkHttp 读超时 60s → 180s，慢网络也能下完整大包
- 比较 `0.1.0.1` → `0.1` 时按段比较正确识别降级（已装用户不会收到"升级"通知）

### 🔧 其他
- 应用内「数据同步」迁入设置页（替代首页入口）
- 「基本设置」位于设置页第一入口，与「数据同步」平级
- 主页顶栏只保留「⚙️设置」入口，去掉同步/主题切换按钮（移入设置页）

### 🐛 历史问题修复
- 修复「点击直播间闪退」：移除缺权限的 `setWakeMode`
- 修复「横屏全屏退出需按两次返回」：Auto 全屏时第一次返回直接退出播放器
- 播放器资源全面兜底释放 + 可选配置逐项 try/catch

> 上一版 v0.1（2026-08-28）首次发布，下方保留当时的详细功能说明作为历史。

## v0.1（2026-08-28）

### 🔄 应用内检查更新
- 设置页新增「检查更新」，点击后自动查询仓库 Release 是否有新版本
- 已是最新时提示「已是最新版本（v当前版本）」
- 发现新版本时展示版本号、体积、发布时间与更新说明，点「下载并安装」自动下载 APK 并唤起系统安装界面
- 下载过程带实时进度条，下载前自动清理历史安装包
- 仓库迁移至 **cookie-kangd/dtv_X**，发布产物统一命名为 `dtv-X-<版本号>.apk`

### 🎬 画质档位（默认最高）
- **解除 1080p 解码分辨率上限**：不再限制最大解码分辨率，「最高」档放开到流本身的分辨率，并强制选取流中最高码率，优先保证画面清晰
- 设置 → 基本设置新增「播放画质」四档：**最高（默认）/ 高（1080p）/ 中（720p）/ 低（480p）**
- 网络较差时可调低档位换取流畅度；切换档位会立即重建播放器生效
- 播放页内各平台自身的清晰度选项（原画/高清等）不受影响，仍可单独调节

### 🃏 小卡片模式（默认开启）
- 基本设置新增「小卡片模式」开关，默认开启
- 开启后：首页与斗鱼/虎牙/抖音/B站列表由 2 列切换为 **3 列**，卡片封面、圆角、头像、字号、间距同步缩小，一屏可展示约翻倍的直播间
- 关闭后恢复原来的 2 列大卡片；加载骨架屏也会跟随切换，避免切换时跳动

### ⚙️ 设置页改版
- 首页（关注列表）顶栏**最右侧**新增 ⚙️「设置」入口
- **「记住栏目」默认开启**：首次安装即为开启状态，无需手动勾选
- **数据同步迁入设置页**：与「基本设置」平级，功能完全一致，仅入口位置调整；首页顶栏不再显示同步图标
- **主题模式迁入基本设置**：提供「跟随系统 / 亮色 / 暗色」三档切换，首页顶栏的日夜模式图标已移除

### 基本设置包含
- **记住栏目**：在斗鱼、虎牙、抖音、B站切换栏目（如网游竞技、单机热游）时自动记住，再次打开该平台恢复上次选择的栏目（支持二、三级分类）
- **小卡片模式**：默认开启，3 列紧凑卡片，一屏展示更多直播间
- **播放画质**：默认最高（不限制分辨率 + 强制最高码率），可选高/中/低
- **主题模式**：跟随系统 / 亮色 / 暗色
- **全局颜色**：内置 13 种预设强调色 + 一键恢复默认，应用于选中栏目高亮、弹幕昵称颜色、顶栏标题、底部导航选中态等

### 🚀 播放器优化
- **缓存**：点播/回放启用 128MB LRU 磁盘缓存；直播走纯网络源，避免缓存引入额外延迟
- **播放**：按直播/点播分别配置缓冲策略（直播 12s/24s 起播更快更实时，点播 30s/60s 抗网络抖动）；接入音频焦点，来电或其他媒体播放时自动让出
- **内存**：限制最大解码分辨率 1080p，避免超高码率流导致 OOM 与掉帧；离开播放页时先摘除监听再 `stop → clearMediaItems → release`，彻底释放解码器与底层资源
- **省电**：耳机断开自动暂停；使用 GPU 友好的视频缩放模式；离开播放页复位屏幕常亮

### 🐛 问题修复
- **修复「点击直播间进入播放就闪退」**：上一版误加了 `setWakeMode(WAKE_MODE_NETWORK)`，它需要 `android.permission.WAKE_LOCK`，缺权限时起播获取锁的瞬间抛 `SecurityException` 直接崩溃。已移除该调用（`PlayerView` 的屏幕常亮已足够），并把所有播放器可选配置逐项 try/catch、缓存初始化失败自动回退网络源、播放器构建失败降级为最简播放器，确保不再因播放器配置异常而闪退
- 修复「退出播放器需要按两次返回」：横屏时会自动进入全屏，此前第一次返回只是退出全屏（手机仍横屏、界面几乎无变化），现在横屏全屏状态下按一次返回即直接退出播放器
- 修正播放器销毁顺序，避免退出瞬间回调导致的状态抖动

### 📦 工程
- 版本号调整为 v0.1（versionCode 3）
- GitHub Actions：推送后自动构建 Release APK 并发布到 Releases，tag 固定为 `v0.1`（同名发布自动替换）
- 清理已迁移入口的冗余导航（`Screen.Sync`）与无引用的代码

> 本项目仅用于学习与技术交流；播放内容与相关数据来自第三方平台接口，版权归属第三方。
