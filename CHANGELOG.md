# DTV Mobile 更新日志

## v0.1（2026-08-28）

### ⚙️ 设置页改版
- 首页（关注列表）顶栏**最右侧**新增 ⚙️「设置」入口
- **「记住栏目」默认开启**：首次安装即为开启状态，无需手动勾选
- **数据同步迁入设置页**：与「基本设置」平级，功能完全一致，仅入口位置调整；首页顶栏不再显示同步图标
- **主题模式迁入基本设置**：提供「跟随系统 / 亮色 / 暗色」三档切换，首页顶栏的日夜模式图标已移除

### 基本设置包含
- **记住栏目**：在斗鱼、虎牙、抖音、B站切换栏目（如网游竞技、单机热游）时自动记住，再次打开该平台恢复上次选择的栏目（支持二、三级分类）
- **主题模式**：跟随系统 / 亮色 / 暗色
- **全局颜色**：内置 13 种预设强调色 + 一键恢复默认，应用于选中栏目高亮、弹幕昵称颜色、顶栏标题、底部导航选中态等

### 🚀 播放器优化
- **缓存**：点播/回放启用 128MB LRU 磁盘缓存；直播走纯网络源，避免缓存引入额外延迟
- **播放**：按直播/点播分别配置缓冲策略（直播 12s/24s 起播更快更实时，点播 30s/60s 抗网络抖动）；接入音频焦点，来电或其他媒体播放时自动让出
- **内存**：限制最大解码分辨率 1080p，避免超高码率流导致 OOM 与掉帧；离开播放页时先摘除监听再 `stop → clearMediaItems → release`，彻底释放解码器与底层资源
- **省电**：耳机断开自动暂停；使用 GPU 友好的视频缩放模式；离开播放页复位屏幕常亮

### 🐛 问题修复
- 修复「退出播放器需要按两次返回」：横屏时会自动进入全屏，此前第一次返回只是退出全屏（手机仍横屏、界面几乎无变化），现在横屏全屏状态下按一次返回即直接退出播放器
- 修正播放器销毁顺序，避免退出瞬间回调导致的状态抖动

### 📦 工程
- 版本号调整为 v0.1（versionCode 3）
- GitHub Actions：推送后自动构建 Release APK 并发布到 Releases，tag 固定为 `v0.1`（同名发布自动替换）
- 清理已迁移入口的冗余导航（`Screen.Sync`）与无引用的代码

> 本项目仅用于学习与技术交流；播放内容与相关数据来自第三方平台接口，版权归属第三方。
